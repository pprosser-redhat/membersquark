apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "membersdb-helm.fullname" . }}-test-connection"
  labels:
    {{- include "membersdb-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "membersdb-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
