apiVersion: v1
kind: Service
metadata:
  name: {{ include "membersdemo-helm.fullname" . }}
  labels:
    {{- include "membersdemo-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "membersdemo-helm.selectorLabels" . | nindent 4 }}
