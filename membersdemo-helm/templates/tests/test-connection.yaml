apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "membersdemo-helm.fullname" . }}-test-connection"
  labels:
    {{- include "membersdemo-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "membersdemo-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
